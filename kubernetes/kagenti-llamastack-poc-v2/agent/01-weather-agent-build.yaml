# Weather Agent Build - AgentBuild CR
# Builds the weather A2A agent image from source
apiVersion: agent.kagenti.dev/v1alpha1
kind: AgentBuild
metadata:
  labels:
    app.kubernetes.io/created-by: llamastack-poc
    kagenti.io/framework: LangGraph
    kagenti.io/protocol: a2a
    kagenti.io/type: agent
  name: weather-service-build
  # namespace: set by script
spec:
  mode: dev
  source:
    sourceRepository: "github.com/kagenti/agent-examples.git"
    sourceRevision: "main"
    sourceSubfolder: "a2a/weather_service"
    sourceCredentials:
      name: github-token-secret
  pipeline:
    namespace: kagenti-system
    parameters:
    - name: SOURCE_REPO_SECRET
      value: github-token-secret
    - name: registry-secret
      value: internal-registry-secret
  buildOutput:
    # image and imageRegistry set by script with envsubst
    image: "${NAMESPACE}/weather-service"
    imageTag: "v0.0.1"
    imageRegistry: "image-registry.openshift-image-registry.svc:5000"
